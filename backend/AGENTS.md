# backend/ — Legacy Express.js

Scope: `backend/`

This is a legacy Express.js dev remnant, not part of the active architecture (Android + Next.js + Firebase Data Connect).

## Rule
- **Read-only.** Do not modify anything in this directory unless the user has explicitly asked for work here.
- If a task seems to require touching `backend/`, stop and confirm with the user first — it likely means the task should target `web/src/app/api/` (Server Actions) or `dataconnect/schema/` instead.
